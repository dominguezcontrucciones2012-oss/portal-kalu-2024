async function testSearch() {
  const ceds = ["1234567", "12345678", "709200", "v-709200"];
  // We can just iterate or guess her cedula, or better, we can modify the client portal screen to log the user object to the console!
}
